class PokemonType {
  constructor(id, name, pokemons, color, icon) {
    this.id = id;
    this.name = name;
    this.pokemons = pokemons;
    this.color = color;
    this.icon = icon;
  }
}
export default PokemonType;
